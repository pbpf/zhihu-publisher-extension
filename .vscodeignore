node_modules/**/docs/**
node_modules/**/test/**
node_modules/**/samples/**
# 保留全部依赖，避免缺失 cosmiconfig 等 puppeteer 运行时依赖
# 如果后续需要瘦身，可改为打包到单文件并仅保留 puppeteer 及其依赖树
!dist/**
src/**
**/*.ts
**/*.map
.vscode/**
.vscode-test
!README.md
!CHANGELOG.md
# package.json / LICENSE 默认包含